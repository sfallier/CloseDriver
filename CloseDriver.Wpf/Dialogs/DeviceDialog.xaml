<Window x:Class="CloseDriver.Wpf.Dialogs.DeviceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:models="clr-namespace:CloseDriver.Core.Models;assembly=CloseDriver.Core"
        xmlns:local="clr-namespace:CloseDriver.Wpf.Dialogs"
        mc:Ignorable="d"
        Title="Select Bluetooth Device" Height="430" Width="400" MinHeight="330" MinWidth="300"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        Loaded="Window_Loaded" Closing="Window_Closing">
    <Window.Resources>
        <ObjectDataProvider x:Key="ScanModeValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:BleScanMode"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding StatusMessage}" Margin="0,0,0,8" TextWrapping="Wrap">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="#444"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsStatusError}" Value="True">
                            <Setter Property="Foreground" Value="#B00020"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,6,0" Foreground="#444"/>
            <ComboBox ItemsSource="{Binding Source={StaticResource ScanModeValues}}"
                      SelectedItem="{Binding ScanMode}"
                      Width="160" Margin="0,0,8,0"/>
            <Button Content="Rescan" Command="{Binding StartScanningCommand}" Width="60"/>
        </StackPanel>

        <ListBox Grid.Row="2" ItemsSource="{Binding DiscoveredDevices}" SelectedItem="{Binding SelectedDevice}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="5">
                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="RSSI: " Foreground="Gray"/>
                            <TextBlock Text="{Binding Rssi}" Foreground="Gray"/>
                            <TextBlock Text=" dBm" Foreground="Gray"/>
                            <TextBlock Text="  " Foreground="Gray"/>
                            <TextBlock Text="{Binding Kind}" Foreground="#666" FontSize="11"/>
                        </StackPanel>
                        <TextBlock Text="{Binding Id}" FontSize="10" Foreground="LightGray"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Select" Command="{Binding SelectDeviceCommand}" Click="SelectButton_Click" Width="75" Margin="0,0,10,0"/>
            <Button Content="Cancel" Click="CancelButton_Click" Width="75"/>
        </StackPanel>
    </Grid>
</Window>
