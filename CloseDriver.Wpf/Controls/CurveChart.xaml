<UserControl x:Class="CloseDriver.Wpf.Controls.CurveChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" BorderBrush="LightGray" BorderThickness="1" Background="White" SnapsToDevicePixels="True">
            <Canvas x:Name="ChartCanvas" ClipToBounds="True">
                <!-- Axis lines -->
                <Line x:Name="Baseline" Stroke="LightGray" StrokeThickness="1" X1="0" Y1="0" X2="0" Y2="0"/>
                <Polyline x:Name="CurveLine" Stroke="SteelBlue" StrokeThickness="2" Fill="Transparent" Points="0,0"/>
                <Polyline x:Name="AreaFill" Stroke="Transparent" Fill="#200A4A7C" Points="0,0"/>
            </Canvas>
        </Border>
        <TextBlock x:Name="XAxisLabel" Grid.Row="1" HorizontalAlignment="Center" FontSize="10" Foreground="Gray" Margin="0,2,0,0"/>
    </Grid>
</UserControl>
