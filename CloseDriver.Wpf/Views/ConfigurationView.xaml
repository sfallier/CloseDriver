<UserControl x:Class="CloseDriver.Wpf.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CloseDriver.Wpf.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="SectionGroupBox" TargetType="GroupBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style x:Key="PlaceholderText" TargetType="TextBlock">
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Text" Value="(coming soon)"/>
        </Style>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  Padding="10">
        <StackPanel Orientation="Vertical">

            <GroupBox Header="Motor Parameters" Style="{StaticResource SectionGroupBox}">
                <TextBlock Style="{StaticResource PlaceholderText}"/>
            </GroupBox>

            <GroupBox Header="Power Curve" Style="{StaticResource SectionGroupBox}">
                <TextBlock Style="{StaticResource PlaceholderText}"/>
            </GroupBox>

            <GroupBox Header="Regen Settings" Style="{StaticResource SectionGroupBox}">
                <TextBlock Style="{StaticResource PlaceholderText}"/>
            </GroupBox>

            <GroupBox Header="Drive Mode Settings" Style="{StaticResource SectionGroupBox}">
                <TextBlock Style="{StaticResource PlaceholderText}"/>
            </GroupBox>

            <GroupBox Header="Feature Settings" Style="{StaticResource SectionGroupBox}">
                <TextBlock Style="{StaticResource PlaceholderText}"/>
            </GroupBox>

            <GroupBox Header="Display Settings" Style="{StaticResource SectionGroupBox}">
                <TextBlock Style="{StaticResource PlaceholderText}"/>
            </GroupBox>

            <GroupBox Header="Safety Settings" Style="{StaticResource SectionGroupBox}">
                <TextBlock Style="{StaticResource PlaceholderText}"/>
            </GroupBox>

            <GroupBox Header="PID Parameters" Style="{StaticResource SectionGroupBox}">
                <TextBlock Style="{StaticResource PlaceholderText}"/>
            </GroupBox>

            <GroupBox Header="Read Only Parameters" Style="{StaticResource SectionGroupBox}">
                <TextBlock Style="{StaticResource PlaceholderText}"/>
            </GroupBox>

        </StackPanel>
    </ScrollViewer>
</UserControl>
