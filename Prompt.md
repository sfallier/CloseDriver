Let's generate a backlog of tasks for a new project, suitable for use with a Ralph Wiggum loop.  The goal is to create a Windows desktop application that can read and set settings for FarDriver electric motor controllers over Bluetooth LE.  We want to reach parity with the functionality of the FarDriver phone app, but with UI appropriate for a PC.  I'd like to write as much code in C# as possible,  but we can interop with other languages if necessary.  We'll do this incrementally, breaking the project down into the smallest, testable tasks possible.  The output of this planning mode session will be a backlog of small tasks, with instructions for verification of each task.  I have an idea of the general of tasks to implement and technology to use, but please suggest alternatives.  I am particularly interested in suggestion on how to automate verification.  

Here is a list of initial tasks.  If it's possible to break this down into smaller steps, please do so.

- [ ] Create a new, empty WPF project in this directory.  Use XAML to define UI layout.
- [ ] Create a new github repository for the project, called 'CloseDriver' (matches the name of the directory we're working from) with an initial commit of the empty project.
- [ ] Add a button 'Scan For  Devices' that will dock to the top left of our application window.
- [ ] Add code to scan for and enumerate Bluetooth LE devices
- [ ] Add a modal dialog that displays enumerated devices in a scrollable vertical list.  The 'Scan For Devices' button should open this dialog.  Upon opening, it will scan for devices on Bluetooth and populate the list.  We'll refer to this modal dialog as the Device Dialog from here on out.
- [ ] Add a 'Select Device' button to the bottom Device Dialog.  This button is only enabled when an device has been selected from the scroll list.  Clicking this button will track the specified device for all device-specific interactions through the rest of the software, and should close the dialog.
- [ ] Add a button 'Log To File' that will dock to the top right of the main window.  This button will only be enabled once a device has been selected.  Once logging has started, the button label should change to 'Stop Logging'.  While logging, the software should write all data we read from bluetooth to a file.  The purpose of this logged data will be for unit testing and verification for later tasks as we reverse engineer the communication protocol for the FarDriver controller.
- [ ] Reverse engineer read data to identify individual settings.  I'm not sure how to do this.  There's a public github C++ repository to communicate with a FarDriver controller over serial port here: https://github.com/jackhumbert/fardriver-controllers  I'm hoping it talks the same protocol over Bluetooth, but I would like your input on how we can incrementally test and modify to figure this out.  There's an existing, but very poorly translated from Chinese, program for iOS and Android that I can reference to enumerate possible settings and match against the data we're reading if that helps. This task will require much further break down once we reach this phase.
- [ ] Develop UI to display all the settings from the controller.  We can flesh out the design of the UI once we've developed code to read all possible settings.  This task will require much further break down once we reach this phase.
- [ ] Develop UI and code to edit settings and write them back to the controller.  This may also require some reverse engineering.  The official FarDriver phone app requires creating an account and binding it to the individual controller to enable writing settings.  This makes me concerned that there's some secret key required from the manufacturer to write settings.  Other people have written software to do this, so hopefully the FarDriver account creation and controller binding mechanics are not technically necessary. This task will require much further break down once we reach this phase.

